// Prisma schema (initial template)
// MVP は LocalStorage 管理のため DB は未使用。
// 将来クラウド同期を実装する際にモデルを追加する。

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
