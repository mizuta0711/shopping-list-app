# Stage 1 設計レビュー結果

- 実施日時: 2026-05-06 00:38
- 対象: `docs/features/20260505_phase10.2-stores.md`
- モード: feature (Stage 1)

## 総合判定: ⚠️ 条件付き承認

両者から実質的な指摘あり。**画面圧迫（縦方向領域）** と **削除 UX の重さ**、**既存ユーザーへの周知** が共通の懸念点。Stage 1 確定済み (§3-X) の判断のうち 2 件（削除挙動 / 履歴画面のタブ配置）に再考材料あり、ユーザー判断が必要。

## code-reviewer（設計整合性 + UI/UX設計）

| # | 重要度 | 内容 | 対象 | 対応状況 |
|---|--------|------|------|---------|
| R1 | **高** | リストタブバー (44-48px) + ScopeTabs (約 44px) の二段重ねで 375px 端末の縦方向が 88-92px 潰れる。ヘッダー (52px) + AddItemForm (56px) + BottomNav (56px) と合わせるとアイテム表示領域が約 130-160px しか残らない | §3-3 画面レイアウト | 未対応 / Stage 1 修正 or §3-X 追加で要再検討 |
| R2 | **高** | §4 ブラウザ評価計画で「マイグレーション中断（途中でブラウザを閉じる）」「v2 互換モード継続中の次回起動の挙動」シナリオが欠落 | §4-1 シナリオ | 未対応 / §4-1 に 2 件追加 |
| R3 | 中 | 技術負債.md FUTURE-1 の「着手判断指標が満たされた根拠」が §1 概要に明示されていない。親 Phase 10 設計書 §2 タスク #7 のステータス未更新 | §1 背景 / 親設計書 | 未対応 / §1 に根拠 1 文追記 + 親 §2 タスク #7 を 🟡 実施中 へ |
| R4 | 中 | リスト削除に `window.confirm` を採用 → UX-2「破壊的操作 UI 分散」をさらに悪化（追加箇所） | §3 操作フロー E | 未対応 / 技術負債.md UX-2 状態欄に「Phase 10.2 で追加」を記録 |
| R5 | 中 | UX-3「UI モデル変更時のユーザー周知方針」が未定義のまま、本フェーズが既存ユーザーに「タブバー突然出現」という変化を与える。コーチマーク等の周知手段が §3 に一切ない | §3 操作フロー A | 未対応 / 周知方針を §3 に追加 or「Stage 1 では許容」と明記 |
| R6 | 中 | リストタブバーの ARIA 構造未記述。ScopeTabs と同階層に 2 つの `role="tablist"` が並ぶ。`aria-label` での区別が必要 | §3-3 画面レイアウト | 未対応 / `aria-label="リスト選択"` / `aria-label="表示期間"` の区別を §3-3 に明記 |
| R7 | 低 | §4 UX 評価観点に「複数端末での同期競合 UX」が欠落（削除中リストへの追加試行など） | §4-2 UX 評価観点 | 未対応 / §4-2 に追加 |
| R8 | 低 | `/lists` 一覧でのアクティブリスト表示が「ピン留め or バッジで区別」と曖昧。`/sets` との見た目が乖離するリスク | §3-3 `/lists` レイアウト | 未対応 / 具体スタイル（左端 ● バッジ + `bg-gray-50`）を確定 |
| R9 | 低 | §3-6 「JSON エクスポート/インポート」で「インポート時のリスト ID 衝突は新規 ID 採番（要検討）」と未確定。Phase 7 設計書のスキーマ更新範囲が不明 | §3-6 既存機能との相互作用 | 未対応 / Stage 2 スコープへ送る + §2 タスクに追加 |

## product-advisor（企画・UX体験）

| # | 優先度 | 観点 | 指摘内容 | 対応状況 |
|---|--------|------|---------|---------|
| P1 | **高** | データ喪失リスク | 物理削除 + `window.confirm` 単体は誤タップに弱い。スマホ片手操作で「とりあえず OK」を押すユーザーが一定数いる。クラウド同期で他端末にも tombstone 伝播する破壊的操作に対し、`window.confirm` だけでは保護不十分 | 未対応 / **§3-X-7 の確定（物理削除 + confirm）を再検討する余地あり** |
| P2 | **高** | 画面領域圧迫 | ヘッダー(48) + リストタブバー(44-48) + ScopeTabs(40) = 130-136px 固定占有。BottomNav(56) + AddItemForm(56) を引くとアイテム表示が約 130px。リスト 3-4 件並ぶとアイテムがほぼ見えない初期状態 | 未対応 / R1 と同件 |
| P3 | **高** | 発見性 | タブバー末尾の「+」は水平スクロール時に画面外に隠れる。新規追加の主要導線として機能性に疑問 | 未対応 / `/lists` ヘッダー側に追加導線を一本化する案を検討 |
| P4 | 中 | 既存ユーザーへの UI 変化 | アップデート後にタブバーが現れる。リスト 1 件時に「意味不明な帯」に見える | 未対応 / R5 と関連 |
| P5 | 中 | セット共通化の合意 | 「スーパー専用セット」要望は使い始めて即出る可能性。今ロックすると Phase 10.3 で設計変更必要 | 未対応 / 仕様の将来拡張余地を §6 注意事項に明記 |
| P6 | 中 | 履歴 UX | 履歴をリスト別表示にすると「全店舗の振り返り」が出来ない。履歴閲覧ユースケースはメイン画面と異なる | 未対応 / **§3-X-6 の確定（タブバーをそのまま履歴に配置）を再検討する余地あり**。「全リスト」タブを先頭に追加する案 |
| P7 | 低 | JSON 整合性 | インポート時のリスト ID 衝突解決が「新規 ID 採番（要検討）」のまま | 未対応 / R9 と同件 |
| P8 | 低 | 多次元管理の認知負荷 | リスト × スコープ の 2 次元を同時管理する複雑さ | 設計上不可避 / 受容 |

### product-advisor の改善提案

| # | 提案 | 優先度 |
|---|------|------|
| PA-1 | 削除 UI をカスタムダイアログ + 30 秒 Undo トースト（ソフト削除 + 遅延物理削除）に格上げ | **高** |
| PA-2 | リスト 1 件時はタブバー非表示（2 件目以降で出現） | **高** |
| PA-3 | タブバー末尾「+」を廃止し、新規追加導線を `/lists` ヘッダーボタンに一本化 | 中 |
| PA-4 | 履歴画面に「全リスト」タブを先頭に追加 | 中 |
| PA-5 | アップデート初回起動時のみコーチマーク 1 枚 | 中 |

## 両者の指摘が競合する場合

| # | 競合内容 | code-reviewer | product-advisor | ユーザー判断 |
|---|---------|--------------|----------------|-------------|
| C1 | リスト削除 UX | UX-2 を悪化（横断課題追記が必要、ただし設計自体は許容範囲） | 物理削除 + confirm では誤削除ダメージ大、Undo トースト推奨 | **§3-X-7 を再考: 「Undo トースト方式に変更する」か「物理削除を維持する」か** |
| C2 | 履歴画面のリスト切替 UX | （特に異論なし。整合性 OK） | リスト別表示だと「全店舗振り返り」ができず体験低下。「全リスト」タブを先頭に追加すべき | **§3-X-6 を再考: 「全リスト」タブを追加するか** |

## 技術負債.md への登録候補（新規）

| # | 候補 | 内容 |
|---|------|------|
| UX-4 | 複数 tablist 並置による AT ナビ複雑化 | 本フェーズ以降、メイン画面に `role="tablist"` が 2 つ並存。`aria-label` 区別だけでなく、実機スクリーンリーダー検証が複数 Phase に影響するため横断課題として登録推奨 |

## サマリー

- 高優先度: 4 件（うち 2 件は両者一致 = 画面圧迫 / 削除ダメージ）
- 中優先度: 7 件
- 低優先度: 6 件
- 競合: 2 件（C1 削除挙動 / C2 履歴タブ）
- 新規横断課題候補: 1 件（UX-4）
