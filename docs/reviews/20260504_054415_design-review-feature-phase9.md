# Stage 1 設計レビュー結果 — Phase 9 クラウド同期

- 実施日時: 2026-05-04 05:44
- 対象: `docs/features/20260504_phase9-cloud-sync.md`
- モード: feature (Stage 1)

## 総合判定: ⚠️ 条件付き承認

重要度「高」の指摘がいくつかあるが、Stage 1 設計書のアップデートで対応可能。
本ファイルの指摘事項を設計書に反映後、Stage 2 に進む。

---

## code-reviewer（設計整合性 + UI/UX設計）

| # | 重要度 | 内容 | 対象 | 対応状況 |
|---|--------|------|------|---------|
| C1 | 高 | id 生成方式（実装は `crypto.randomUUID()`、設計書1条に「cuid + updatedAt」と乖離あり）。Stage 2 でスキーマ確定前に方式を明示せよ | `3-1 方針表` | ✅ 設計書に「ID は実装の実態に合わせ UUID v4。Prisma 側も `@default(uuid())` で統一」と明記 |
| C2 | 高 | ログアウト後の再ログインで別ユーザーがログインした場合、前ユーザーのローカルキャッシュが別アカウントのサーバーへ送信されるリスク。マージ API は `userId` 厳格フィルタ前提を明示せよ | `3-1 フロー A / D` | ✅ フロー D に「ログアウト時にローカルデータを削除するオプション（デフォルトは保持）」を追加。マージ時のサーバー側 userId 検証を方針に明記 |
| C3 | 高 | `window.online` プロパティは存在しない。`window.addEventListener('online', ...)` の表記に修正 | `3-1 フロー E` | ✅ 修正 |
| C4 | 中 | ヘッダーアイコン過密（既存4個 + 同期で5個、375px で破綻リスク）。レイアウト検討を明記 | `3-3 メイン画面ヘッダー` | ✅ 「ログイン時のみ表示し、SortMenu 内に統合する案」を方針に追記 |
| C5 | 中 | OnboardingModal の「データはこの端末のみに保存される」文言が、ログイン後は不正確になる。出し分けまたは削除方針を明記 | `OnboardingModal.tsx` | ✅ 「Phase 9 適用時にオンボーディングを更新する旨」を方針に追記。実装は Phase 9 内で対応 |
| C6 | 中 | `/login` の「← 設定」ヘッダーは設定画面以外からの遷移時に問題。`router.back()` 既定 + フォールバック先 `/` を明示 | `3-3 ログイン画面` | ✅ 設計書に明記 |
| C7 | 中 | `deleteItem` の同期方式が未定義。tombstone（論理削除）か hard delete か、Stage 1 で方針確定せよ | `1. 概要 方針表`、`3-1 フロー B` | ✅ 「MVP は hard delete + 削除側勝ち（updatedAt 比較で削除イベント勝利）方式」を明記。tombstone の必要性は Phase 9.1 として将来検討 |
| C8 | 低 | `5. ブラウザ評価計画` がエッジケース未記載 | `5.` | ✅ 主要エッジケースを箇条書きで追加 |
| C9 | 低 | `3-5` のレビュー記録ファイル名がプレースホルダー | `3-5` | ✅ 本レビュー後に実ファイル名で更新 |

---

## product-advisor（企画・UX体験）

| # | 優先度 | 観点 | 指摘内容 | 推奨対応 | 対応状況 |
|---|--------|------|---------|---------|---------|
| P1 | 高 | LWW データ消失リスク | 別端末で同時編集すると無警告でデータ消失する可能性。買い忘れ等、利用シーンと直接結びつく | 競合検知時にトーストで「別端末で更新があり最新で上書きしました」を通知 | ✅ Stage 1 受け入れ条件に「同期で上書きが発生したら事後トースト表示」を追加 |
| P2 | 高 | 初回ログイン時のマージサマリー欠如 | LocalStorage→サーバーマージ時にユーザーは何が起きたか分からない | 「X件をマージ、Y件をサーバーから取得」のサマリートースト | ✅ フロー A の手順7に追加 |
| P3 | 高 | QR/URL 共有という代替案の検討記録なし | 「家族で共有」需要には Google アカウント必須は重い。検討理由を明記 | 「Phase 9 は端末をまたぐ個人利用のみ。家族共有は将来別フェーズで検討」を方針に明記 | ✅ 方針表に判断根拠を追記 |
| P4 | 中 | エクスポート/インポートとクラウド同期の使い分け不明確 | UI 上で2つの保存方法が並列に並ぶ | 設定画面に「クラウド同期中は JSON エクスポートはバックアップ用途」など説明文を追加 | ✅ 設計書に明記、実装時に説明テキストを追加 |
| P5 | 中 | ログアウト後のローカルデータ残存への懸念 | スマホを他人に貸す際に内容が見える、混乱を招く | ログアウト確認ダイアログに「このデバイスのデータも削除」オプション追加 | ✅ フロー D に組み込み済み（C2 と統合） |
| P6 | 中 | オフラインキューの永続化方式が未定 | リロードでキュー消失すれば「自動送信」要件を満たせない | 「LocalStorage に永続化」を Stage 1 方針に明記 | ✅ 方針表に「オフライン中の編集自体が LocalStorage に保存される（既存の persist 経由）。別途キュー管理は不要、サーバー同期は最新状態を都度送信する pull-driven 方式」と明記 |
| P7 | 低 | ヘッダーアイコン配置の操作性懸念 | アイコン増による圧迫 | C4 と同じ。SortMenu 内に統合 | ✅ C4 で対応 |
| P8 | 低 | Phase 9 優先度根拠未記載 | 開発コスト大の割に需要検証なし | 企画書に仮説として明記 | ⚪ 設計書に「仮説段階の機能であり、MVP リリース後の利用ログを見て着手を判断する」を追記。企画書本体は今回更新せず |

---

## 競合する指摘なし

両者の指摘は補完的（code-reviewer は技術整合性、product-advisor は UX 妥当性）。直接の対立は無し。
