# Stage 1 設計レビュー結果

- 実施日時: 2026-05-06 11:43
- 対象: docs/features/20260506_phase10.4-set-list-binding.md
- モード: feature (Stage 1)

## 総合判定: ⚠️ 条件付き承認

- code-reviewer 高 3 / 中 4 / 低 1
- product-advisor 高 2 / 中 4 / 低 2
- 競合: なし（両者は相補的な観点。共通指摘 1 件あり）
- §3-X 要決定事項 X1〜X6 が未確定 → Stage 2 着手前にユーザー判断必須
- 新規横断課題候補: MAINT-5（ListPickerSheet 系の共通化）

---

## code-reviewer（設計整合性 + UI/UX設計）

| # | 重要度 | 内容 | 対象 | 対応状況 |
|---|--------|------|------|---------|
| C1 | 高 | DB 外部キー `ShoppingSet.listId` の `ON DELETE Restrict` 採用根拠が未明記。先行 UPDATE → DELETE のパターンとの整合性を §3-4 か §7 に追記すべき | §3-4（行 397）/ §3-5 | 未対応 |
| C2 | 高 | API Zod スキーマ整合: `ShoppingSetSchema` に `listId` 追加時、旧クライアントからの push が 400 になる懸念。`listId` を `optional()` 扱い + サーバー側で未分類補完する旨を §3-4/§3-6 に明示。タスクにも「Zod スキーマ更新」を追加 | §3-4 / §3-6 / §1 後方互換 | 未対応 |
| C3 | 高 | `useInitialMerge` の sets merge 時に `listId` を `remappedUnclassifiedIds` でリマップする必要がある（items と同様）。§3-6 と タスク #13 に未記載 | §3-6 / タスク #13 | 未対応 |
| C4 | 中 | BottomNav 5 タブ寸法・font-size の現行整合検証不足。`text-[10px]` の視認性、`inset-x-6` アクティブバーが 75px タブで中央に収まるかを確認・記載 | §3-3 BottomNav 寸法ブロック（行 221-223） | 未対応 |
| C5 | 中 | `SetListPickerSheet` と `ListMoveSheet` のコンポーネント共通化未検討（選択ハイライト有無の違いのみ）。`selectedId?: string` prop 追加で共通化できないか検討、または別コンポーネント維持の理由を §3-3 に記載 | §3-3 SetListPickerSheet（行 283-304） | 未対応 |
| C6 | 中 | `SetPickerSheet` の `aria-label` 記述が現行実装（`aria-labelledby`）と乖離。「`aria-labelledby` で step 別ヘッダー id 参照」に修正 | §3-3 SetPickerSheet（行 338） | 未対応 |
| C7 | 中 | `/lists` の戻るボタン削除後のヘッダーパディング・並びが未記述。`SetListView` の `px-4 py-3` パターンに合わせる旨を明記 | §3-3 /lists 軽微変更（行 364-376） | 未対応 |
| C8 | 低 | §5 ブラウザ評価計画が未記入。Stage 1 確定時に v1→v2 マイグレーション / `/sets` グルーピング / `SetListPickerSheet` / `SetPickerSheet` 上下二段 / リスト削除 confirm 文言 / BottomNav 5 タブ / 設定リンク削除 の 7 観点を最低限記入 | §5（行 471-473） | 未対応 |

### §3-X Stage 2 着手前必須判定（code-reviewer）

| # | Stage 2 前必須 | 根拠 |
|---|---------------|------|
| X1 BottomNav アイコン | 必須 | `BottomNav.tsx` 実装に直接反映 |
| X2 共通セクション位置 | 必須 | 並び順ロジックに影響 |
| X3 SetPicker サブヘッダー | 必須 | UI 実装仕様に直結 |
| X4 confirm 文言 | 必須（低リスク） | 文字列定数として明記 |
| X5 listId 不整合補完 | 必須 | C2（API Zod）と直結 |
| X6 マイグレ失敗時挙動 | 必須 | LS マイグレ実装の分岐に直結 |
| X7 コーチマーク | 不要 | 推奨案で確定可 |

---

## product-advisor（企画・UX体験）

| # | 優先度 | 観点 | 指摘内容 | 対応状況 |
|---|--------|------|---------|---------|
| P1 | 高 | C1 認知摩擦 | SetPickerSheet の「その他」から他リスト紐付けセットを選んだとき、追加先がアクティブリストになる非対称性。第 2 段（アイテムチェック）画面に「→ {リスト名}に追加」サブテキストを常時表示することを §3-3 に明記 | 未対応 |
| P2 | 高 | 「未分類」vs「共通」表記揺れ | `/sets` 一覧では「共通」、SetListPickerSheet では「未分類」と表記されている。同一オブジェクトで表記統一が必要（推奨: SetListPickerSheet 内も「🗂️ 共通（デフォルト）」） | 未対応 |
| P3 | 中 | BottomNav 視認性 AC | 受け入れ条件に「375px 端末でラベルが切れず表示される」を追加。3 文字以内に揃える検討も Stage 2 で（C4 と関連） | 未対応 |
| P4 | 中 | SetPicker「その他」情報設計 | アクティブが「未分類」のとき、上部「現在のリスト」= 未分類紐付けセットとなる。X2（共通セクション位置）の判断に SetPickerSheet 版の挙動も含めて議論し設計書に明記 | 未対応 |
| P5 | 中 | 設定画面リンク削除の移行摩擦 | コーチマーク不要の根拠を「BottomNav は視覚的に目立つ」「設定画面はリンク探しの場所でない」等で補強。または初回 1 回の軽量ツールチップ検討 | 未対応 |
| P6 | 中 | confirm 文言の M=0 分岐 | 操作フロー D の confirm が「N 件のアイテム + M 件のセット」だが、M=0 のとき「0 件のセット」表示になる。M=0 はセット文言を省略する分岐を追記 | 未対応 |
| P7 | 低 | グルーピング順序の明示 | `/sets` 一覧の「ユーザー作成リスト作成順」が createdAt 昇順か降順か、ホーム ListTabs 表示順と一致するかを受け入れ条件に明記 | 未対応 |
| P8 | 低 | JSON インポート時の listId 不整合 | Phase 10.4 以降エクスポート JSON を旧クライアントにインポートする逆行ケースの挙動を §3-5 に追記（実用上稀だが受け入れ条件として記録） | 未対応 |

### §3-X 要決定事項への意見（product-advisor）

| # | 推奨案の妥当性 | 意見 |
|---|--------------|------|
| X1 | 条件付き妥当 | リスト = `LayoutList` の方が意味的には正確（`ShoppingCart` は視認性は高いがホームから消えると連想弱まる） |
| X2 | 最下部固定を支持 | 「どこにも属さないもの」は意味的に最下部が正しい |
| X3 | サブヘッダーあり支持 | フラット表示は判別困難 |
| X4 | N 件+M 件支持（P6 分岐追加が条件） | 連鎖は明示すべき |
| X5 | 未分類補完支持 | 拒否は旧クライアントデータ消失リスク |
| X6 | バックアップ復元 + トースト支持 | 起動拒否は過剰 |
| X7 | 不要を条件付き支持 | 軽量ツールチップ余地は残す |

### 次に検討すべき問い（product-advisor）

- C1 の非対称性をアプリ内 UI でどこまで明示するか
- 「未分類」と「共通」の表記統一は将来のリスト名変更可能化を見据えてどう扱うか
- Phase 10.3（カテゴリ）導入時に SetPickerSheet の上下二段構成がカテゴリ軸と衝突しないか先に構造整理する価値あり

---

## 両者の重複指摘（共通課題）

| 共通課題 | code-reviewer | product-advisor |
|---------|--------------|----------------|
| BottomNav 5 タブの視認性検証不足 | C4（寸法・font-size） | P3（受け入れ条件追加） |

→ 両指摘を統合し「Stage 2 で実機 375px 検証 + 受け入れ条件記入」として対応。

## 両者の指摘が競合する場合

なし（相補的）

---

## 技術負債.md への登録候補

| 候補 ID | 内容 | 関連既存課題 |
|---------|------|------------|
| MAINT-5 候補 | `ListMoveSheet` / `SetListPickerSheet`（本フェーズ新設）/ 将来の類似 BottomSheet が「リスト行の単一選択」として構造的に重複。共通 `ListPickerSheet` 基底コンポーネントへの集約を検討 | MAINT-4（reconcile ジェネリック化と同趣旨） |

---

## 推奨アクション

1. **§3-X の X1〜X6 をユーザーに確認**（X7 は推奨で確定）
2. C2（Zod 後方互換）/ C3（useInitialMerge sets リマップ）/ C1（ON DELETE 根拠）を §3-4・§3-6 に追記
3. P1（追加先サブテキスト）/ P2（「共通」表記統一）/ P6（M=0 分岐）/ P7（作成順明示）を設計書に反映
4. C4 + P3（BottomNav 視認性）を受け入れ条件に追加
5. C8（評価計画 7 観点）を §5 に草案記入
6. C5（SetListPickerSheet 共通化）の方針を §3-3 に明記
7. MAINT-5 を技術負債.md に登録するかユーザーに確認
