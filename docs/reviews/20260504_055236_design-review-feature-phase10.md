# Stage 1 設計レビュー結果 — Phase 10 セット機能・店舗別リスト・自動分類

- 実施日時: 2026-05-04 05:52
- 対象: `docs/features/20260504_phase10-sets-stores-categories.md`
- モード: feature (Stage 1)

## 総合判定: ⚠️ 条件付き承認（スコープ調整あり）

product-advisor から「3機能の同時導入は『シンプルに使える』コンセプトを毀損する」「店舗別リスト・自動分類は需要根拠が弱い」との指摘あり。
これを踏まえ、**Phase 10.1（セット機能）のみ Stage 2 → 実装に進む方針** に縮退。Phase 10.2 と Phase 10.3 は本設計書では Stage 1 で残しつつ、実装は MVP リリース後の利用ログ・ユーザーニーズ確認を経て判断する位置づけに変更。

---

## code-reviewer（設計整合性 + UI/UX設計）

| # | 重要度 | 内容 | 対応 |
|---|--------|------|------|
| C1 | 高 | ジェスチャー競合（dnd-kit 250ms delay vs 編集 800ms 長押し） | ✅ ロングプレス廃止。編集は **行末の編集アイコン or 「カテゴリ別表示」モード時のみ表示するボタン** に変更 |
| C2 | 高 | STORAGE_VERSION の採番ルール（Phase 9 と衝突可能性） | ✅ 「各サブフェーズで `STORAGE_VERSION` を独立に採番。Phase 9 が先に着手された場合は v3、その後 Phase 10.2 が v4、Phase 10.3 が v5」と方針表に明記 |
| C3 | 高 | ヘッダーアイコン過密の最終状態確認 | ✅ Phase 10.2 のリスト切替を**ドロップダウンではなく ScopeTabs と同居する「リストタブバー」**として再設計（P6 と統合）。最終的なヘッダー要素は `Cart + RefreshCw + SortMenu + History + Settings` のまま |
| C4 | 中 | `listId` 追加に伴う `ShoppingItem` 型・`buildItem` 等への影響範囲 | ✅ 設計書に「`activeListId` は ShoppingMainView の state で保持。`addItem` 系は内部で `useShoppingStore.getState().activeListId` 経由ではなく、引数 `listId` を必須化」と明記 |
| C5 | 中 | 重複スキップキーに `listId` 追加が必要 | ✅ Phase 10.2 適用時のスキップキーは `(name, listId, scope, status)` と明記 |
| C6 | 中 | `SortKey` に `CATEGORY` 追加方針が未記述 | ✅ カテゴリ別表示は **`SortKey` ではなく独立した「表示モード」フラグ** として分離（P10 と統合） |
| C7 | 中 | `/sets` と `/lists` のルーティング方式が混在 | ✅ どちらも独立ページ（`/sets`, `/lists`）。設定画面は単なるエントリポイント（リンクのみ）で内部にセクションは持たない |
| C8 | 中 | セット内アイテムの `scope` 帰属が未定義 | ✅ セットは `scope` を持たず、追加時のアクティブタブを採用 |
| C9 | 低 | ブラウザ評価計画にジェスチャー競合シナリオが欠落 | ✅ Stage 2 ブラウザ評価計画で「編集トリガーがドラッグと競合しないこと」を必須項目化 |
| C10 | 低 | 1ファイル設計書 → サブフェーズ分割の Stage 2 運用 | ✅ 「本設計書は Stage 1 のみ。Stage 2 はサブフェーズ独立で `20260504_phase10.1-sets.md` のように別ファイルで作る」と §7 注意事項に明記 |

---

## product-advisor（企画・UX体験）

| # | 優先度 | 観点 | 指摘 | 対応 |
|---|--------|------|------|------|
| P1 | 高 | 3機能同時導入はコンセプト毀損 | 設定画面・ヘッダー・ソートメニュー全てが一気に複雑化 | ✅ **スコープ縮退**: Phase 10.1（セット）のみ実装着手対象。10.2 / 10.3 は Stage 1 に残すが「ニーズ検証後に判断」と明記 |
| P2 | 高 | 店舗別リストの代替手段（既存タブ・プレフィックスで代用可能） | ✅ Phase 10.2 を**保留扱い**。MVP リリース後にユーザーが「今日 / また今度」を店舗別に流用しているか観察してから判断 |
| P3 | 高 | 自動分類の必要性根拠なし | リスト件数が数十件で本当にカテゴリ表示が必要か不明 | ✅ Phase 10.3 を**保留扱い**。「リストが平均何件で困るか」を観察してから着手 |
| P4 | 高 | 誤分類のロングプレス修正は発見性が低い | ✅ ロングプレス廃止。**カテゴリ別表示モード時のみ、行末に編集アイコン**を表示 |
| P5 | 高 | セットと JSON エクスポートの機能重複 | ✅ セットの差別化を「**ワンタップ追加の速度・買い物中の即応性**」に集中していると §1 概要で明記。JSON は永続バックアップ、セットは日常運用と棲み分け |
| P6 | 中 | ヘッダードロップダウン置換の発見性が低い | ✅ **リストタブバー（ScopeTabs と同様の見た目で水平スクロール可能）**に変更（C3 と統合） |
| P7 | 中 | セット空状態に CTA がない | ✅ 空状態に「セットを作成する」ボタン配置を受け入れ条件に追加 |
| P8 | 中 | セットの全リスト共通設計の根拠なし | ✅ 設計書に「MVP 段階では全リスト共通。リスト別セットへの拡張は将来検討」と明記 |
| P9 | 中 | ジェスチャー競合の受け入れ条件記述 | ✅ C1 / C9 と統合 |
| P10 | 中 | カテゴリ別ソートが既存ソートと異質 | ✅ 「**表示モード**」トグルとして分離。並び替えメニューと別 UI（C6 と統合） |
| P11 | 低 | サブフェーズ独立設計書化 | ✅ Stage 2 でファイル分割を明記（C10 と統合） |
| P12 | 低 | セット編集にプレビュー欠如 | ✅ 受け入れ条件に「入力中に『X品が登録されます』のプレビュー」を追加 |

---

## 競合する指摘なし

両者の指摘は補完的。特に **「3機能同時実装はコンセプト毀損」（P1）と「ヘッダー過密問題」（C3）が一致**していて、対応として「Phase 10.2 と 10.3 を保留 + 10.1 から優先実装」という縮退方針に落ち着いた。
