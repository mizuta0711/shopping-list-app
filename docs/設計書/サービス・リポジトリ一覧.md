# サービス・リポジトリ一覧

## 概要

MVP は LocalStorage のみで Service 層は未使用だった。Phase 9 でクラウド同期 + 認証を導入し、API route から直接 Prisma Client を呼ぶ薄い層 + ヘルパーユーティリティ群を追加した。本格的な Service 層 / Repository 層は将来フェーズで分離する想定。

---

## 1. 認証関連 (`src/lib/auth.ts`)

| 関数 / 定数 | 説明 |
|-----------|------|
| `handlers` | Auth.js v5 の `{ GET, POST }` ハンドラー。`src/app/api/auth/[...nextauth]/route.ts` から re-export される |
| `auth()` | サーバー側 (route handler / Server Component) で現在のセッションを取得 |
| `signIn(provider)` | プログラム的なサインイン |
| `signOut()` | プログラム的なサインアウト |

設定: `PrismaAdapter(prisma)` + `session.strategy: "database"` + Google provider + `pages.signIn: "/login"`。Session callback で `user.id` を `session.user.id` に注入。

---

## 2. API ヘルパー (`src/lib/api/`)

| ファイル | エクスポート | 説明 | 実装Phase |
|---------|------------|------|----------|
| `sync-helpers.ts` | `requireSession()` | `auth()` 呼び出し + 401 / `UNAUTHORIZED` 早期 return。判別ユニオン `{ error } | { userId }` を返す | Phase 9 |
| `sync-helpers.ts` | `badRequest(message, fields?)` | 400 / `INVALID_INPUT` レスポンスを返す | Phase 9 |
| `sync-helpers.ts` | `internalError()` | 500 / `INTERNAL_ERROR` レスポンスを返す | Phase 9 |
| `sync-schemas.ts` | `ShoppingItemSchema`, `SyncPullQuerySchema`, `SyncPushSchema`, `SyncMergeSchema` | Zod スキーマ。すべて `.strict()` で余分フィールド (userId 偽装) を拒否 | Phase 9 |
| `dto.ts` | `toDTO(item: PrismaShoppingItem): ShoppingItemDTO` | Prisma モデル → API レスポンス DTO への変換。`userId` を除外し `Date` を ISO string に変換 | Phase 9 |

---

## 3. データアクセス

Phase 9 では Repository 層を作らず、API route から直接 `prisma` (`src/lib/db.ts`) を呼ぶ。route 内で `prisma.$transaction` を使い LWW 判定 + tombstone upsert を1トランザクションで実行する。

将来的に同期ロジックの再利用ニーズが出た場合、`src/lib/services/syncService.ts` への分離を検討。

---

## 4. クライアント側同期サービス (`src/features/sync/services/`)

| ファイル | エクスポート | 説明 | 実装Phase |
|---------|------------|------|----------|
| `reconcile.ts` | `reconcile({ local, serverChanges, serverDeletes, rejected })` | 純粋関数。LWW 判定 + 上書き発生件数を返す | Phase 9 |
| `syncClient.ts` | `syncClient.pull / push / mergeOnLogin`, `HttpError` | fetch ラッパー。指数バックオフ (1s/2s/4s) + AbortController + 401 handling | Phase 9 |
| `syncOrchestrator.ts` | `createSyncOrchestrator()` (factory) | `start/stop/pullOnce/pushPendingNow/retry/merge`。`useShoppingStore.subscribe` + debounce 1.5s + window online/offline/focus イベント。HMR・SSR 対応。merge は初回ログイン時のサーバーマージで使用 | Phase 9 |

---

## 4. サービス間の依存関係

```
src/app/api/sync/items/route.ts
  ├─ requireSession (sync-helpers)
  ├─ SyncPullQuerySchema / SyncPushSchema (sync-schemas)
  ├─ toDTO (dto)
  └─ prisma (lib/db)

src/app/api/sync/merge/route.ts
  ├─ requireSession (sync-helpers)
  ├─ SyncMergeSchema (sync-schemas)
  ├─ toDTO (dto)
  └─ prisma (lib/db)

src/app/api/auth/[...nextauth]/route.ts
  └─ handlers (lib/auth)
```

---

## 改訂履歴

| 版数 | 日付 | コミット | 内容 | 担当 |
|------|------|---------|------|------|
| 1.0 | 2026-05-04 | (未確定) | 初版作成。Phase 9 で導入した認証ライブラリ・API ヘルパー・DTO 変換層を記載 | Claude Code |
| 1.1 | 2026-05-05 | (未確定) | Phase 9 タスク #8 で追加したクライアント側同期サービス (reconcile / syncClient / syncOrchestrator) を §4 に追記 | Claude Code |
