# テーブル定義書

> このドキュメントは `tools/scripts/generate-table-docs.ts` により自動生成されています。
> 手動編集しないでください。変更は `prisma/schema.prisma` に対して行ってください。

---

## Enum定義

---

## テーブル一覧

| # | テーブル名 | モデル名 | 説明 |
|---|-----------|---------|------|
| 1 | User | User | |
| 2 | Account | Account | |
| 3 | Session | Session | |
| 4 | VerificationToken | VerificationToken | |
| 5 | ShoppingItem | ShoppingItem | |
| 6 | DeletionTombstone | DeletionTombstone | |

---

## 1. User

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|---|------|----------|------|
| id | String (CUID) | NO | uuid() | 主キー |
| name | String | YES | - |  |
| email | String | NO | - | 一意 |
| emailVerified | DateTime | YES | - |  |
| image | String | YES | - |  |
| createdAt | DateTime | NO | now() |  |
| updatedAt | DateTime | NO | @updatedAt |  |

**リレーション:**
- `accounts` → `Account` (1:N)
- `sessions` → `Session` (1:N)
- `shoppingItems` → `ShoppingItem` (1:N)
- `tombstones` → `DeletionTombstone` (1:N)

---

## 2. Account

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|---|------|----------|------|
| id | String (CUID) | NO | uuid() | 主キー |
| userId | String | NO | - |  |
| type | String | NO | - | / "oauth" など |
| provider | String | NO | - | / "google" |
| providerAccountId | String | NO | - |  |
| refresh_token | String | YES | - |  |
| access_token | String | YES | - |  |
| expires_at | Int | YES | - |  |
| token_type | String | YES | - |  |
| scope | String | YES | - |  |
| id_token | String | YES | - |  |
| session_state | String | YES | - |  |

**インデックス:**
- `[provider, providerAccountId]` - UNIQUE
- `[userId]`

**外部キー:**
- `userId` → `User.id` (CASCADE DELETE)

---

## 3. Session

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|---|------|----------|------|
| id | String (CUID) | NO | uuid() | 主キー |
| sessionToken | String | NO | - | 一意 |
| userId | String | NO | - |  |
| expires | DateTime | NO | - |  |

**インデックス:**
- `[userId]`

**外部キー:**
- `userId` → `User.id` (CASCADE DELETE)

---

## 4. VerificationToken

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|---|------|----------|------|
| identifier | String | NO | - |  |
| token | String | NO | - | 一意 |
| expires | DateTime | NO | - |  |

**インデックス:**
- `[identifier, token]` - UNIQUE

---

## 5. ShoppingItem

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|---|------|----------|------|
| id | String (CUID) | NO | - | / クライアント発行 UUID v4 |
| userId | String | NO | - | / 所有ユーザー |
| name | String | NO | - | / アイテム名 |
| scope | String | NO | - | / "TODAY" \| "LATER" |
| status | String | NO | - | / "PENDING" \| "PURCHASED" |
| order | Int | NO | - | / 同一 scope 内の表示順 |
| createdAt | DateTime | NO | - | / クライアント発行のタイムスタンプ |
| updatedAt | DateTime | NO | - | / LWW 判定キー（クライアント発行） |
| purchasedAt | DateTime | YES | - | / PURCHASED に遷移した時刻 |

**インデックス:**
- `[userId, updatedAt]`
- `[userId, scope, status]`

**外部キー:**
- `userId` → `User.id` (CASCADE DELETE)

---

## 6. DeletionTombstone

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|---|------|----------|------|
| id | String (CUID) | NO | uuid() | 主キー |
| userId | String | NO | - | / 削除を行ったユーザー |
| itemId | String | NO | - | / 削除された ShoppingItem の id |
| deletedAt | DateTime | NO | now() | / サーバー時刻 |

**インデックス:**
- `[userId, itemId]` - UNIQUE
- `[userId, deletedAt]`

**外部キー:**
- `userId` → `User.id` (CASCADE DELETE)

---

## 改訂履歴

| 版数 | 日付 | コミット | 内容 | 担当 |
|------|------|---------|------|------|
| auto | 2026-05-04 | - | schema.prisma から自動生成 | generate-table-docs.ts |
