# 技術負債と将来課題

複数 Phase 横断の改善候補。各 Phase の機能設計書 (`docs/features/`) には書ききれない、長期的な技術負債や横断的な改善案を集約する。

## 運用ルール

- **追加タイミング**: コードレビュー・設計レビュー・ブラウザテストで「次フェーズ検討」「許容（将来対応）」とした項目を、各 Phase 完了時にここへ転記する
- **着手判断**: 新しい Phase の計画時に本ドキュメントを参照し、優先度の高いものを次 Phase のスコープに取り込む。優先度が同等なら関連性の高い Phase に巻き取る
- **削除タイミング**: 解消したら該当行に「✅ 解消 (Phase X.Y で対応、コミット xxxxxxx)」と記載。半年経過した解消済み項目は本ドキュメント末尾の「アーカイブ」セクションへ移動
- **Phase 固有の改善案**: 該当 Phase の機能設計書 §7「将来タスク」に書く。本ドキュメントは複数 Phase 横断のもののみ

---

## 1. UX / アクセシビリティ

| # | 内容 | 優先度 | 関連箇所 | 出典 | 状態 |
|---|------|-------|---------|------|------|
| UX-1 | ヘッダーアイコンボタン全般のタッチターゲットが 36×36px (推奨 44px 未満)。Phase 12 で **History / Settings はボトムナビ化により 56px へ移行（部分解消）**。残る RefreshCw / SortMenu / 同期ドット用 ShoppingCart は 36px のまま | 中 | `src/features/shopping/components/ShoppingMainView.tsx` のヘッダー | Phase 9 ブラウザテスト #2 (2026-05-05) | 部分対応（Phase 12 で 2/5 解消、Phase 11 で `≡` ハンドルが 44px 対応 1 増） |
| UX-2 | 削除・破壊的操作の確認 UI が `window.confirm` ベースで分散（`SettingsView.handleReset`, `SetEditView.handleDelete` 等）。Phase 11 で削除はアンドゥ付きトースト方式へ移行したが、**残る window.confirm を全廃しインライン確認 / アンドゥトーストへ統一する横断方針**が未定義 | 中 | 各 View の delete/reset ハンドラ | Phase 11 Stage 1 レビュー（2026-05-05） | 未対応 |
| UX-3 | 操作ステップ増加・操作モデル変更を伴う UI 変更時の**ユーザー周知方針**（コーチマーク / ツールチップ / リリースノート / 初回ヒント）が未定義。Phase 11 でドラッグハンドル `≡` 新設 + スワイプ追加という二重変化を入れるため、既存ユーザーの学習コストが課題 | 中 | アプリ全体 | Phase 11 Stage 1 レビュー（2026-05-05） | 未対応 |

---

## 2. パフォーマンス

| # | 内容 | 優先度 | 関連箇所 | 出典 | 状態 |
|---|------|-------|---------|------|------|
| PERF-1 | `ShoppingMainView` を `memo` でラップしていない。子コンポーネント（行・モーダル・シート）が増えるにつれ再レンダリングコストが目立ち得る | 低 | `src/features/shopping/components/ShoppingMainView.tsx` | Phase 9 code-review #13 (2026-05-05) | 未対応 |

---

## 3. 保守性 / リファクタリング

| # | 内容 | 優先度 | 関連箇所 | 出典 | 状態 |
|---|------|-------|---------|------|------|
| MAINT-1 | `formatRelative(iso)` が `AccountSection.tsx` と `SyncStatusSheet.tsx` で重複定義されている。`src/lib/utils/formatRelative.ts` 等に共通化したい | 低 | 上記2ファイル | Phase 9 code-review #14 (2026-05-05) | 未対応 |
| MAINT-2 | `src/lib/api/withAuth()` ラッパーの本格実装（現状は `sync-helpers.ts` の `requireSession()` で代用）。route handler が増えたタイミングで横断的に整備する | 中 | `src/lib/api/`、`.claude/01_development_docs/04_error_handling_design.md` | Phase 9 tech レビュー T2 (2026-05-04) | 未対応 |

---

## 4. インフラ / 運用

| # | 内容 | 優先度 | 関連箇所 | 出典 | 状態 |
|---|------|-------|---------|------|------|
| OPS-1 | API パスバージョニングを `/api/v1/...` で一斉導入する判断。Phase 9 時点では `v1` プレフィクスなしで統一しているが、破壊的変更を入れたい時に `/v1/` redirect + 新規 `/v2/` を導入する戦略を立てる | 低 | `src/app/api/`, `.claude/01_development_docs/03_api_design.md`, `docs/設計書/API一覧.md` | Phase 9 tech レビュー T10 (2026-05-04) | 未対応 |

---

## 5. 機能追加判断待ち（観察データ収集中）

| # | 内容 | 着手判断指標 | 関連設計書 | 状態 |
|---|------|-------------|-----------|------|
| FUTURE-1 | Phase 10.2 店舗別リスト | 既存ユーザーの「今日/また今度」を店舗別流用している声・問い合わせが3件以上、または 1 ユーザーあたりの平均アクティブアイテム件数が異なる店舗種別を示唆するパターン | [親設計書 §3-B](../features/20260504_phase10-sets-stores-categories.md) | 観察中 |
| FUTURE-2 | Phase 10.3 自動分類 | アクティブユーザーの平均 PENDING 件数が 15 件超 / カテゴリ別表示要望が3件以上 | [親設計書 §3-C](../features/20260504_phase10-sets-stores-categories.md) | 観察中 |
| FUTURE-3 | Phase 10.1b セットのクラウド同期化 | Phase 10.1a リリース後、ログインユーザーから「セットも同期してほしい」要望、または 10.1a の利用ログでセット作成・編集が継続的に行われていることを確認 | [Phase 10.1a 設計書 §8](../features/20260504_phase10.1-sets.md) | 10.1a 完了後に Stage 2 設計予定 |

---

## アーカイブ（半年経過した解消済み項目）

（該当なし）

---

## 改訂履歴

| 版数 | 日付 | コミット | 内容 | 担当 |
|------|------|---------|------|------|
| 1.0 | 2026-05-05 | (未確定) | 初版作成。Phase 9 のレビュー / ブラウザテストで「Phase 横断課題」と判断した5件 (UX-1, PERF-1, MAINT-1, MAINT-2, OPS-1) を登録 | Claude Code |
| 1.1 | 2026-05-05 | 17b1c7a | §5「機能追加判断待ち」を新設し FUTURE-1（Phase 10.2 店舗別リスト）/ FUTURE-2（Phase 10.3 自動分類）/ FUTURE-3（Phase 10.1b セットのクラウド同期化）を登録。Phase 10 親設計書の保留指標を本書に集約 | Claude Code |
| 1.2 | 2026-05-05 | 3000a53 | UX-1 を Phase 12（ボトムナビ化）で部分解消（History/Settings の 2/5 を 56px へ移行）。残る 3 アイコン（RefreshCw / SortMenu / 同期ドット）は今後別フェーズで対応 | Claude Code |
| 1.3 | 2026-05-05 | (未確定) | Phase 11 Stage 1 レビューを受け UX-2（破壊的操作 UI 統一方針）/ UX-3（UI 変更時のユーザー周知方針）を新設。Phase 11 で `≡` ハンドル 44px 対応により UX-1 がさらに 1 件解消 | Claude Code |
